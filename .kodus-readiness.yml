---
criteria:
  # Kodus does not recognise Python/uv tooling, so a supplemental assessment
  # script reports the criteria below. This one cannot be evaluated from the
  # local filesystem at all:
  bundle-analysis: false  # N/A — Python library, no frontend bundle
