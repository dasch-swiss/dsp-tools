# Configuration of https://pre-commit.com/
# after adding a new hook, run `pre-commit install`

repos:
  - repo: https://github.com/psf/black
    rev: 23.3.0
    hooks:
      - id: black
        # It is recommended to specify the latest version of Python
        # supported by your project here, or alternatively use
        # pre-commit's default_language_version, see
        # https://pre-commit.com/#top_level-default_language_version
        language_version: python3.9

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-docstring-first
      - id: check-merge-conflict
      - id: no-commit-to-branch
        args: [--branch, main]

  - repo: local
    hooks:
      - id: check-links
        name: check-links
        language: system
        files: ".+\\.md"
        entry: poetry exec check-links

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.35.0
    hooks:
    - id: markdownlint

# We're still experimenting with different ways to use markdownlint pre-commit hooks.
# Overview of possibilities:
#  - local: docker run -v $PWD:/workdir ghcr.io/igorshubovych/markdownlint-cli:latest --config .markdownlint.yml --ignore CHANGELOG.md "**/*.md"
#  -  github.com/igorshubovych/markdownlint-cli
#       markdownlint - Checks the style of Markdown/Commonmark files.
#       markdownlint-fix - Fixes the style of Markdown/Commonmark files.
#       markdownlint-docker - Checks the style of Markdown/Commonmark files.
#       markdownlint-fix-docker - Fixes the style of Markdown/Commonmark files.
#  - github.com/markdownlint/markdownlint
#     markdownlint - Run markdownlint on your Markdown files
#     markdownlint_docker - Run markdown lint on your Markdown files using the project docker image
